# Phase 9.9 问题台账

## 本轮已修复

1. 多镇共用 `xxgk.shbsq.gov.cn` 后，来源调度测试仍按域名单行查询；已增加大场 `region_code` 条件。
2. External 关闭且检索命中时没有写入助手查询事件，运营看板少计；已补齐成功检索事件。
3. Phase 7/9.5 测试仍断言“办事”、仅大场和五项后台导航；已同步 Phase 9.9 的“服务”、三镇与商业运营入口。
4. Playwright 的 `**/api/**` 路由会在 Vite 开发模式误拦截 `/src/api/*.ts`；已增加 pathname 守卫。
5. 来源扫描测试仍模拟旧同步 `/discover`；已迁移到异步 `/discover-jobs` 返回结构。
6. 详情视觉验收写死已从真实环境清理的演示 slug；详情测试已改为读取当前已发布内容，不要求恢复模拟数据。

## 未解决

- 真实来源 7 个，低于 10 个目标。
- PUBLISHED 16，低于 35～50 目标；顾村、庙行 LOCAL 均为 0。
- 搜索尚未完成独立的地区 scope 联动。
- 三个 region code 仍待外部权威编码页面二次人工核对。
- 商业配置写 UI、真实服务商录入和真实订单 UI 流程尚未完成。
- 没有支付商户凭据，真实支付正确保持阻塞。
- 本轮没有新的 External 授权，30 问 DeepSeek REAL 验收未执行。
