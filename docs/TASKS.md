# 任务清单

更新时间：2026-07-28

## 运行环境验证记录

- [x] 已解决由 Python 3.9 创建的 AI 虚拟环境与当前 Uvicorn/Click 依赖不兼容的问题；Python 3.9 不受支持。
- [x] 已使用 Python 3.13 重建 AI 服务 `.venv`；项目最低支持版本仍为 Python 3.11，Python 3.13 为当前已验证版本。
- [x] FastAPI `/docs` 与 `/health` 已验证可访问。
- [x] Spring Boot Swagger `http://127.0.0.1:8080/swagger-ui/index.html` 已验证可访问。
- [x] PowerShell 启停脚本已记录实际监听 PID，区分已有服务与本轮所有权，并可按项目命令行安全清理 5173/5174/8080/8001。
- [x] 前后端默认地址、CORS 与后端到 AI 的回环连接已统一为 `127.0.0.1`；内部 AI 请求显式绕过系统代理。

## Phase 0 — 已完成

- [x] 检查实际工作区、Git、Node、Java、Maven 与 Python。
- [x] 创建并核验项目简报、架构、API、任务和 UI 指南。
- [x] 当前 VS Code 工作区作为仓库根目录，无 `jianda/` 嵌套目录。

## Phase 1 — 已完成

- [x] 两个 Vue 3 + TypeScript + Vite 应用可类型检查和生产构建。
- [x] Spring Boot 3 / Java 17 后端可测试、打包和 H2 启动。
- [x] FastAPI AI 服务可导入、启动，MockProvider 测试通过。
- [x] 共享包、MySQL Docker Compose、Dockerfile、启动脚本和 README。

## Phase 2 — 已完成核心能力

- [x] BCrypt 演示账号、JWT 登录和 `/auth/me`。
- [x] ORG_ADMIN / REVIEWER / PLATFORM_ADMIN 角色写入 JWT。
- [x] 材料与日志查询按机构隔离，平台管理员允许跨机构。
- [x] 公开来源和导入接口使用方法级权限，仅 PLATFORM_ADMIN 可访问；机构管理员返回 403。
- [ ] 平台机构账号管理页面和更细粒度的审核员权限规则。

## Phase 3 — 已完成核心闭环

- [x] PDF/PNG/JPG 类型与 20MB 限制、清理文件名、随机路径保存原件。
- [x] 创建处理任务并调用独立 FastAPI MockProvider。
- [x] 稳定生成字段、原文页码/片段、通俗版、步骤卡片、术语、风险提示和语音稿。
- [x] AI 不可用时任务标记失败并记录服务端根因日志，不影响已发布内容。
- [x] 扫描 PDF 自动使用本地 Tesseract OCR；文本 PDF 使用 PyMuPDF，混合 PDF 仅识别扫描页。

## Phase 4 — 已完成核心闭环

- [x] 原文与结果左右对照、字段修改与确认，审核页展示真实导入标题、原文和引用片段。
- [x] 未确认字段阻止审核；无审核记录阻止发布。
- [x] 审核、发布、撤回写入操作日志。
- [x] 已发布列表连接真实材料接口，可查看和撤回，不再使用静态数组。
- [x] 后端集成测试覆盖上传 → 处理 → 字段确认 → 审核 → 发布 → 公开读取。
- [ ] 操作日志页面改为实时 API（后端查询接口已具备）。

## Phase 5 — 已完成核心展示能力

- [x] H5 首页、分类、详情、18/20/22/24px 字号、语音播放/暂停/停止、原文和收藏。
- [x] 首页、列表、详情和收藏连接公开 API；新发布内容可按后端 slug 查看。
- [x] 权威资讯隐藏不适用的补贴资格/材料模板，动态展示风险提示、术语、来源和原文。
- [x] 375px Playwright 实际流程验证通过。
- [ ] 收藏列表改为后端查询。

## Phase 6 — 已完成核心闭环

- [x] `ContentCollector` 接口、`FixtureCollector` 与 `ManualImportCollector`。
- [x] 三条离线 fixture：反诈提醒、高血压日常管理、社区养老服务安排。
- [x] 权威来源新增、启停、域名白名单、重复 URL/正文拦截和导入记录。
- [x] fixture/手工导入接入同一 AI → 字段确认 → 审核 → 发布 → H5 → 撤回流程。
- [x] MockProvider 按公开信息主题稳定生成，结果可重复测试。
- [x] 平台管理员专属菜单与路由；机构管理员的前端权限页和后端 403 均已验证。
- [x] 后端集成测试覆盖公开信息完整生命周期；Playwright 覆盖真实平台端与 H5 闭环。
- [ ] 已发布内容的修订版本管理；当前可撤回后以新 URL/正文重新导入。
- [ ] 真实网页采集器；按当前范围继续使用 fixture/手工导入，不抓取任意网站。

## Phase 7 — 商业化界面与体验优化（进行中）

两份 Phase 7 规范合并为以下单一实施清单；导航和详情布局以 Phase 7.1 为准，页面范围、视觉方向、工程优化和验收以 Phase 7 总规范为准。

### Phase 7.1 — 统一返回导航与状态恢复

- [x] 两端路由统一补充 `title`、`showBack`、`backTo` 元信息；根页、列表页和登录页不显示返回。
- [x] H5 新增共享 `AppTopBar` 和 `useSafeBack`，站内访问优先返回历史，刷新或直接访问时使用安全 fallback。
- [x] 机构端扩展现有 `PageHeader`，统一返回、面包屑、状态和操作区，不创建重复页面头。
- [x] 原文页回到对应详情；审核、发布等详情页回到业务列表；成功操作使用明确目标导航。
- [x] 离开未保存的审核/发布内容前确认，确认按钮和返回按钮满足键盘与无障碍要求。
- [x] 返回列表后保留筛选条件、分页和滚动位置；启动前已存在的 Phase 6 权限规则保持不变。

### Phase 7.2 — 用户 H5 信息层级与可访问体验

- [x] 首页补齐问候/日期、搜索、大字/朗读快捷入口、最多六个分类、今日重点、办事指南和权威资讯层级。
- [x] 办事指南详情突出适用对象、申请条件、材料、时间地点、步骤、提醒、联系与来源，并提供固定操作栏。
- [x] 权威资讯详情采用独立布局，突出权威来源、日期、三句话摘要、重点、术语、提醒和适用人群。
- [x] 字号 18/20/22/24、语速、收藏持久化；正文默认至少 18px、点击区域至少 48px并支持安全区。
- [x] 补齐加载骨架、空内容、网络失败、已撤回、原文不可用、语音不可用和无搜索结果状态。
- [x] 375px 与 768px 下不横向滚动，底部操作不遮挡正文，直接访问详情可安全返回。

### Phase 7.3 — 机构端商业化界面

- [x] 优化登录、工作台、材料列表、来源管理、导入、审核和已发布页面的信息密度与主次层级。
- [x] 审核页保持左侧原文、右侧结构化内容，增强来源定位、确认进度、风险提示和保存反馈。
- [x] 发布、撤回、审核等关键动作提供清晰确认与成功/失败反馈。
- [x] 补齐 403、加载、错误和空状态；文案保持专业、克制、可信的公共服务 SaaS 风格。
- [x] 1440px 桌面布局稳定；768px 可完成核心操作且不依赖悬停。

### Phase 7.4 — 工程清理与页面性能

- [x] 两端视图改为路由懒加载，拆分机构端大包并复查 chunk 警告。
- [x] 清理可控的 VueUse PURE 警告、未使用依赖和调试输出，不修改第三方依赖源码。
- [x] 保持既定 Vue / Spring Boot / FastAPI 技术栈和真实接口，不修改 Phase 6 后端业务。

### Phase 7.5 — 测试与最终验收

- [x] Playwright 覆盖 H5 首页无返回、详情站内返回、直接访问 fallback、原文返回详情和已撤回状态。
- [x] Playwright 覆盖机构列表无返回、审核详情返回、未保存确认及列表筛选/分页/滚动恢复。
- [x] 实测 375 / 768 / 1440 三档视口，无控制台 error、无关键内容遮挡、无意外横向滚动。
- [x] 两个前端 `typecheck`、`build` 与完整 E2E 通过；后端和 AI 回归测试保持通过。

### Phase 7 已验证基线

- [x] Phase 6 结束时两个前端 `npm run typecheck` 与 `npm run build` 通过。
- [x] Phase 6 结束时后端 `mvn test`（3 条）与 `mvn package -DskipTests` 通过。
- [x] Phase 6 结束时 AI `pytest`（4 条）与模块导入通过。
- [x] Phase 6 Playwright Chromium：平台权限与公开信息全生命周期，共 2 条通过且无控制台 error。

### Phase 7 本轮验收记录

- [x] Browser 插件当前不可用，已按调试规范使用仓库 Playwright Chromium 进行替代验证。
- [x] 机构端入口 chunk 从约 1.04 MB 降至约 99 kB，构建不再出现大 chunk 和 VueUse PURE 警告。
- [x] 截图输出到系统临时目录：`jianda-public-info-h5.png`、`jianda-h5-home-768.png`、`jianda-institution-768.png`、`jianda-institution-1440.png`。


## Phase 7.2 增量 — 消费级 App 功能丰富化（已完成）

### Step 0 — 协作规范

- [x] 将中文进度提交规范写入 `docs/GIT_CONVENTION.md`，README 已增加协作开发入口。
- [x] 每个独立步骤使用中文进度提交，提交前检查变更并运行相关测试，默认不 push。

### Step 1 — App 外壳与一级导航

- [x] 固定五项底部导航：首页、资讯、中央简达助手、办事、我的。
- [x] 新增 `/news`、`/assistant`、`/services`、`/profile` 一级路由，并保持一级页面无返回、二级页面安全返回。
- [x] 中央助手入口突出但克制，点击区域和底部安全区满足适老化要求。

### Step 2 — 首页、资讯与办事专区

- [x] 首页缩短深色区域，增加重要提醒、今日必看、办事快报、权威资讯流和有数据才展示的最近浏览。
- [x] 资讯频道支持搜索、七类频道、最新/重要/收藏筛选、加载更多、已读与收藏状态。
- [x] 办事专区支持搜索、服务对象/类型/机构筛选和事项状态展示，详情可进入助手询问。
- [x] 所有列表仅使用已发布内容，撤回内容不再出现，排序稳定且无随机长数字演示标题。

### Step 3 — 收藏、历史与我的

- [x] 收藏按资讯/办事分类、时间排序，可取消并进入详情；无登录时复用匿名设备与本地持久化。
- [x] 浏览详情自动记录本机历史，支持删除单条、清空和重新进入。
- [x] 我的页面明确游客和本机记录状态，汇总收藏、历史、提醒、设置、帮助、隐私和关于入口。
- [x] 字号、对比度、语速、自动朗读、频道偏好和首页最近浏览开关立即生效并持久化。

### Step 4 — 带来源引用的简达助手

- [x] 助手提供推荐问题、本机会话、文本输入、语音能力检测、清空和历史入口。
- [x] 回答只检索已审核发布内容，展示来源、原文片段、发布时间和详情链接；无依据时明确说明。
- [x] 医疗、金融和政策内容不补充未发布事实，撤回内容不再被引用，为后续 RAG 保留接口边界。
- [x] `AssistantIntegrationTest` 已验证已发布内容引用、撤回排除和无依据安全答复；用户端类型检查与生产构建通过。

### Step 5 — 数据、测试与文档验收

- [x] 幂等补充 4 条办事指南和 8 条权威资讯；指南包含材料、地点、时限、步骤和可追溯原文，不使用前端假数组。
- [x] 覆盖五项导航、频道筛选、办事详情、收藏、历史、偏好、助手引用、撤回过滤和直接访问安全返回。
- [x] 验证 375/768/1440 无横向溢出，浏览器无框架错误覆盖层和相关控制台错误。
- [x] 通过前端类型检查/构建/E2E、后端测试与打包、AI pytest；E2E 最终结果为 6/6 通过。
- [x] 截图输出到系统临时目录：`jianda-h5-assistant-citation-375.png`、`jianda-h5-services-768.png`、`jianda-h5-home-1440.png`、`jianda-institution-768.png`、`jianda-institution-1440.png`。
- [x] 更新 README、API、任务进度、演示流程、存储方式和已知限制；Phase 7.2 达到进入 Phase 8 的功能与测试条件，但本轮未启动部署。

## 当前最早未完成任务

## Phase 7.3 — 人工验收与缺陷修复（已完成）

- [x] 建立人工验收清单、P0—P3 缺陷台账和验收报告骨架。
- [x] 四服务冷启动均返回 200，安全停止后 5173/5174/8080/8001 全部释放。
- [x] 补齐 375/768/1440 页面截图与固定导航、操作栏遮挡检查。
- [x] 检查中文词表、数据真实性、可访问性、空状态和错误降级。
- [x] 修复 8 个 P1 与 6 个高性价比 P2，P0/P1 未修复数均为 0。
- [x] 原文真实下载、安全返回、48px 触控、焦点、偏好持久化和真实操作日志回归通过。
- [x] AI 不可用时 HTTP 503、材料 FAILED、任务 FAILED 与错误原因均经过集成测试。
- [x] 清理 15 条明确的 Phase 7 E2E 残留记录，公开列表与机构列表不再显示随机长数字。
- [x] 前端类型检查/构建、16 项浏览器验收、8 项后端测试、后端打包和 4 项 AI 测试均取得通过证据。
- [x] 输出 `PHASE7_3_ACCEPTANCE_REPORT.md`，结论为“通过：可以进入 Phase 8”。

## 当前最早未完成任务

Phase 7.3 已完成。下一阶段为 Phase 8 部署准备，但必须由用户明确启动；进入前先完成 iPhone Safari、Android Chrome 与目标老年用户真机抽检，并由业务人员确认医疗、反诈和政策文案。Phase 7.3 验收阶段未修改 Docker、Nginx、生产环境变量、服务端口或启停脚本。

## Phase 7.3 局域网真机兼容补充

- [x] 游客 ID 独立封装，依次使用 `crypto.randomUUID`、`crypto.getRandomValues` 和 UUID 格式最终兜底。
- [x] 已有游客 ID 直接复用；明确其只用于匿名收藏、历史和偏好，不作为认证凭证。
- [x] 助手会话消息 ID 复用同一兼容生成函数，普通 HTTP 下不再直接依赖 `randomUUID`。
- [x] 增加 `dev:h5:lan`、同源 `/api` 代理、可配置代理目标和 Spring Boot CORS 显式白名单。
- [x] 生产配置拒绝任意 Origin 通配符，新增允许/拒绝 Origin 的后端集成测试。
- [x] 增加普通 HTTP 缺失或异常 Web Crypto 场景，以及收藏、历史、偏好持久化的 Playwright 回归。
- [x] iPhone 已确认可访问 H5、Swagger 和非空公开接口，局域网与后端端口连通。
- [x] 修复 5174 未配置 `/api` 代理、真机默认 API 地址错误和 JSON 响应未声明 UTF-8。
- [x] 增加 `dev-h5-lan.ps1`，真机代理模式自动清除 `VITE_API_BASE_URL` 并使用 `VITE_PROXY_TARGET`。
- [x] 本机真实 LAN 地址验证首页、资讯、办事、助手数据与同源 API。
- [ ] 待用户在 iPhone Safari 刷新确认修复后页面，并补充 Android Chrome 人工抽检。

## Phase 7.4 增量 — 真机修复与导航重构（代码与自动化已完成）

### 助手真机与检索降级

- [x] 复现浏览器 LAN POST 携带 `Origin` 后被显式 CORS 白名单拒绝的 403；确认 8080 直连和非浏览器 5174 代理均为 200。
- [x] 仅在可信 Vite 开发代理移除浏览器 `Origin`，不放宽生产 CORS，不修改数据库与 Phase 6 业务流程。
- [x] 助手响应增加 `mode: retrieval`，页面明确当前使用已审核内容检索回答并展示引用。
- [x] 区分网络、服务端、繁忙、撤回和格式错误；失败问题保留并支持重新发送。
- [x] LAN Chromium 验证 POST 200、检索模式、两条引用和无控制台错误；AI 服务不可用时仍使用本地检索降级。

### 统一语音与听一听

- [x] 资讯、办事详情和助手回答统一使用浏览器 TTS，支持播放、暂停、继续、停止、中文声音优先和 0.8/1.0/1.2 倍速。
- [x] 长文本按标点和长度分段，路由离开自动停止；Safari 用户手势限制和不支持状态均有中文提示。
- [x] 新增 `/listen` 一级频道，队列数据全部来自已发布接口，包含今日、政策、健康、反诈、养老、收藏和最近收听。
- [x] 收听历史使用 `jianda_listen_history` 本机存储并与公开列表求交集；清除本机资料时一并清除。

### 导航与页面边界

- [x] 五项底部导航调整为：首页、听一听、中央简达助手、办事、我的；删除底部资讯但保留 `/news`。
- [x] `/news` 改为带安全返回首页的二级页，分类与资讯详情继续回到资讯列表。
- [x] 首页收敛为重要提醒、今日必看、频道入口和精简办事快报，不再堆叠完整资讯流。
- [x] 办事页改为行动中心，使用真实详情字段展示适用对象、期限、地点、材料数、步骤数、收藏和事项询问。

### 自动化与人工待确认

- [x] 定向 Playwright 覆盖助手检索、503 重试、语音播放/暂停/继续/倍速/分段/离开停止、收听历史和无能力降级，7/7 通过。
- [x] 覆盖五项导航、资讯安全返回、375/768/1440 无横向溢出和底栏不遮挡；Playwright 截图经人工查看无 P0/P1。
- [x] Browser 插件不可用，按前端调试规范使用 Playwright Chromium 替代；未声称完成真实 iPhone Safari 语音验证。
- [ ] 用户需在 iPhone Safari 最终确认助手发送、引用打开、播放/暂停/继续/停止和系统中文声音；补充 Android Chrome 抽检。

## Phase 7.4 完成后的门禁

完成 iPhone Safari 与 Android Chrome 的 Phase 7.4 人工抽检。代码层建议进入 Phase 8，但 Phase 8 必须由用户明确启动，本轮未修改部署配置。

## Phase 8.1—8.2 部署基线与前端托管

- [x] 生产 profile 强制注入数据库、JWT、上传目录、AI 地址和明确 CORS 来源。
- [x] 后端增加 Actuator health，Compose 为 MySQL、AI 和后端建立健康依赖。
- [x] 两个 Vue 应用统一使用同源 `/api`；机构端补齐 Vite 开发代理。
- [x] 新增多阶段前端镜像和 Nginx 双端口托管，支持 SPA fallback、API 反向代理、上传大小、安全响应头和哈希资源缓存。
- [x] 新增 `.dockerignore`、Phase 8.2 构建完整性测试和部署文档。
- [x] 前端类型检查、生产构建和 3 项无 Docker 部署断言通过。
- [x] 修复后端依赖宿主机 `/fixtures/public-information.json` 导致容器启动失败；默认 fixture 已进入 classpath 和 Spring Boot JAR。
- [x] fixture 支持可选外部路径覆盖；缺失路径明确报错，未配置时稳定回退 classpath。
- [x] 后端 16 项测试、Maven 打包、JAR 资源检查和后端镜像重建通过。
- [x] Docker Compose 四服务全部 healthy，四个健康端点、两端页面、同源 API 和容器内三条 fixture 均已验证。
- [x] Phase 8.2 Playwright 容器 smoke 与构建完整性共 8/8 通过。
- [x] 发布成功页 H5 链接支持 DEV 5174、Docker 同主机无机构端口及 `VITE_H5_BASE_URL` 自定义 HTTPS 地址；Docker 实际点击已进入 `guide-14` 用户详情。
- [x] H5 详情页九类业务字段全部改为读取已发布 `fields`；真实电话支持 `tel:`，地址支持 Clipboard API 与普通 HTTP 降级复制，缺失字段不再显示补贴演示兜底。

## 上传材料真实提取与 Mock 追溯修复

- [x] 移除上传 PDF 时无条件写入“老年补贴申请”固定正文的回退逻辑，后端通过 AI 提取接口调用 PyMuPDF。
- [x] PDF 按真实页码保存 `document_segment`，`raw_text` 与 `page_count` 均来自当前上传文件。
- [x] Mock 指南分析改为使用传入正文的确定性规则；未识别正文返回待人工填写，不伪造字段。
- [x] 保存字段前以 `source_quote` 匹配真实 segment，落库真实 `page_no` 和 `segment_id`，无法追溯时拒绝生成结果。
- [x] 银龄数字生活测试 PDF 已通过正常上传和处理接口重新处理文档 13；原始上传卷和其他发布数据未删除。
- [x] 文档 13 现有 2 页真实正文和 8 个可追溯字段，正文、字段与生成内容均无 80 周岁补贴残留。
- [x] AI 8 项测试、后端 17 项测试、前端类型检查/构建和审核页 Playwright 回归均通过。
- [x] Compose 三个应用镜像重建完成，MySQL、AI、后端和前端四服务均为 healthy。

## 当前最早未完成任务

进入 Phase 8.3，处理内部端口收敛、TLS/域名、容器加固、备份恢复和生产演示数据策略。iPhone Safari、Android Chrome、目标老年用户与业务文案确认仍是公开上线门禁。

## 通用公共服务解析、原文件预览与性能优化

- [x] 增加 v1.1 通用 Schema：对象条件、分时窗口、停办规则、分人群必需/可选材料、费用支付、领取邮寄、相对/分渠道期限和更正信息。
- [x] 所有通用结构条目保留 `source_quote`、`page_no`、`segment_id` 与 `needs_human_review`；未知键和不改变事实的单值/数组表示差异逐项容错，原文、数字和追溯校验仍严格执行。
- [x] v1.1 Prompt 使用本次数据库真实 segment ID，不再用固定示例 ID；为信息密集的短通知保留足够 JSON 输出预算，仍受部署级 token 上限控制。
- [x] 增加通用表格与费用条款的确定性补全和语义去重；材料 5 正确保留周一/周三两个时段、周六上午、周六下午不开放、节假日停办、三类人群材料、两类费用、支付方式、领取/邮寄和相对 30 日规则。
- [x] External 复用进程级 HTTP 连接；相同文件缓存键包含 SHA-256、模型、Prompt 和 Schema 版本，失败不缓存。
- [x] 材料 5 文档 19 真实 v1.1 首次处理成功：Schema 1.1、两条服务窗口、两条费用、三组条件材料及可追溯字段；首次 15072ms、5982 tokens。
- [x] 材料 5 相同配置再次处理命中缓存：35ms、0 tokens，耗时降低约 99.8%；缓存为进程内实现，容器重启后失效。
- [x] 保存原始文件名、MIME、大小和 SHA-256；机构端原文件接口使用 JWT/机构权限，公开端仅在已发布且显式授权时可访问，支持 Range、ETag 和内容 SHA-256。
- [x] 机构端审核页支持“原 PDF/原图 / 提取文本”切换；H5 增加授权原文件全屏预览，通用结构在 375/768px 无横向溢出并保留安全返回。
- [x] Mock `public_news` 改为从传入正文确定性提取警示或服务时间，不再返回与本次正文不一致的固定高血压/反诈/养老引用。
- [x] AI 51/51、Maven、两端 typecheck/build、原文件后端集成测试和 Playwright 58/58 已通过；Browser 插件不可用，按规范使用 Playwright Chromium。
- [x] Docker `ai-service`、`backend`、`frontend` 完整构建通过；MySQL、AI、后端和前端四服务均为 healthy，四个健康地址为 200。
- [ ] 材料 6、7 及同一材料的 v1/v1.1 真实外部模型对比待明确授权将指定测试 PDF 发送给外部 AI 后执行；当前未绕过数据外发审批，也未声称完成。
- [ ] 真实 iPhone Safari、Android Chrome 的原 PDF/原图预览和大字模式仍需人工抽检。

## DeepSeek External Provider 与适老化提示词框架

- [x] 保持 `MockProvider` 为默认实现；Mock 模式无需 API Key，既有接口与测试继续可用。
- [x] 实现 OpenAI-compatible DeepSeek `ExternalLlmProvider`，默认 `deepseek-v4-flash`，支持通过环境变量切换模型。
- [x] 建立“事实提取 → 适老化内容生成”两阶段流程，External 失败时明确报错且不回退 Mock。
- [x] 提示词按 `guide_extract_v1`、`guide_rewrite_v1`、Schema 和 few-shot 示例文件化，当前版本为 `v1`。
- [x] 使用严格 Pydantic Schema 校验字段白名单、非空值、置信度和适老化输出结构。
- [x] 校验 `source_quote` 是对应 `segment_id/page_no` 原文的逐字子串；后端传入真实数据库段落并在入库前继续二次定位。
- [x] Compose 和 `.env.example` 已提供 External 变量框架，Key 默认留空且默认 Provider 仍为 Mock。
- [x] 本地 mock HTTP Server 覆盖正常两阶段、鉴权失败、限流、服务端错误、超时、空响应、非法 JSON、截断、Schema、引用追溯及日志脱敏。
- [x] 银龄活动、健康体检、字段不完整和冲突日期四类离线材料回归均使用不同且可追溯结果。
- [x] 使用本机未提交的 External 配置完成真实 DeepSeek 联调；未读取、输出或提交 API Key。

## DeepSeek 空字段成功与审核页 0/0 修复

- [x] 确认文档 16 原始 PDF、495 字正文、1 页和 1 个 segment 均已正确保存；旧结果的 0 个字段与近空生成内容来自当时实际运行的 Mock Provider 空结果分支。
- [x] External Provider 增加脱敏审计日志，记录 HTTP、请求 ID、结束原因、token、耗时、重试及字段各阶段计数，不记录 Key、认证头、Prompt、原文或完整模型响应。
- [x] External 字段按单项 Schema 校验；`fields=null`、空数组、字段契约错误和全部字段不可追溯时明确失败，阶段 A 失败时不再执行阶段 B。
- [x] `source_quote` 支持仅空格、换行和制表符差异的定位，并映射回 segment 原始连续文字；数字、日期和电话改写仍会被拒绝。
- [x] 日期或时间字段漏掉引用中的具体时间时，使用已定位的真实原文引用保全信息；对明确的“原预约日期调整为新日期”和带具体时间的截止语句进行确定性原文补全，不生成原文不存在的字段。
- [x] 后端在写库前完整验证 AI 结果、必要键、置信度和原文追溯；空字段时 job/document 标记 `FAILED`，不保存生成内容，并保留原 PDF、正文和 segments 以便重试。
- [x] 处理结果页读取真实 `page_count`、segment、job、字段和生成内容；失败显示原因与重新处理入口，删除固定“3 页、12 段”和补贴摘要。
- [x] 审核页对 `WAITING_REVIEW + 0 fields` 显示异常恢复状态，保留正常审核按钮保护。
- [x] 文档 16 经真实 DeepSeek 正常重试后为 `WAITING_REVIEW`：1 页、1 个 segment、10 个可追溯字段、6 类生成内容，最新 job 12 为 `SUCCEEDED`。
- [x] 文档 16 最终包含通知对象、三组预约调整日期、`9月28日18:00以前`、地点、服务时间、电话、材料和风险提示；未生成原文不存在的费用字段。
- [x] AI 40 项、后端 Maven 全套、机构端 typecheck/build 及处理页/审核页 Playwright 3 项通过；四个 Compose 服务均为 healthy，日志敏感词扫描为 0。

## 材料元数据预识别与结构化场次优化

- [x] 新增材料元数据预识别接口；只读取文件名、首页和末页，不创建正式文档或处理任务，临时文件在请求结束后清理。
- [x] 文件名先生成临时标题，PDF/PNG/JPG 选择后自动识别标题、发布机构、文号、机构类型、证据页码、置信度和权威状态；低置信度或冲突时才调用独立低成本 External metadata prompt。
- [x] 权威状态限定为材料内有明确证据的 `DOCUMENT_EVIDENCE`、无法确认的 `UNCONFIRMED` 和候选冲突的 `CONFLICT`，不冒充官网或官方数据库核验。
- [x] 上传页支持识别中、重新识别、证据展示和分类错误提示；人工编辑过的标题或来源不会被迟到响应覆盖，预识别失败仍可手工填写并正式上传。
- [x] source_document 通过 MySQL 兼容 Flyway V3 保存用户确认的来源和元数据，发布页优先展示已确认来源。
- [x] External 提取新增带原文引用的结构化 `sessions`，日期、时间和地点按同一片段成组保存；摘要、步骤和用户端日程优先使用 sessions，不按旧字段数组下标猜测。
- [x] 对完全相同且同源的对象/条件优先按年龄对象与当前健康条件做语义拆分；审核端保留追溯并标记疑似重复，H5 仅对可合并且规范化相同的字段去重。
- [x] 风险提醒按子句拆分、标点与空白规范化后语义去重；不同含义提醒继续保留，“无需空腹”统一显示为“不需要空腹”。
- [x] “预防接种门诊”使用准确术语解释；H5 条件、场次和提醒卡片取消固定高度，长文本自动换行，大字模式不截断。
- [x] 全响应加入安全 `X-Request-Id`，上传页按状态码显示可行动错误；CORS 暴露 request ID，响应不包含堆栈、容器地址或密钥。
- [x] 禁用默认 Spring Security 用户自动配置；JWT 是唯一业务认证链路，Basic 默认用户访问 `/api/documents` 返回 403，机构管理员登录保持正常。
- [x] 真实材料 3 元数据识别为“国庆假期部分专家门诊预约调整告知”、来源“南江区人民医院”、文号“南医门诊〔2026〕27号”，材料证据置信度 0.96。
- [x] 真实材料 4 元数据识别为“秋冬季流感疫苗集中接种登记说明”、来源“海棠街道社区卫生服务中心”、文号“海卫预防〔2026〕09号”，材料证据置信度 0.96。
- [x] 文档 17 使用真实 External Provider 重处理成功：9 个可追溯字段、0 个重复标记；9 月 12 日仅对应 08:00-11:30，9 月 13 日仅对应 13:30-16:30，两场地点均为预防接种门诊。
- [x] 文档 17 最终风险提醒为“不需要空腹”和“不会通过私人二维码收款/要求支付密码”两类；摘要场次无歧义，术语解释准确。
- [x] 已发布材料 4 `guide-18` 保持可访问，公开接口兼容生成两个正确场次；材料 3 文档 16 的 10 个追溯字段审核回归通过。
- [x] AI 45 项、后端 Maven 20 项、两端 typecheck/build、上传/处理/审核/H5 Playwright 12 项全部通过。
- [x] 375、390、768、1440px 与 24px 大字模式无横向溢出、重叠或截断；Browser 插件不可用，按规范使用 Playwright Chromium，真实 iPhone Safari/Android Chrome 仍需人工抽检。
- [x] Compose 三个应用镜像完成重建，MySQL、AI、后端和前端四服务均 healthy；四个 HTTP 检查均为 200。
- [x] External 安全日志包含 provider、model、prompt version、HTTP、request ID、tokens、耗时及 raw/schema/trace/final 字段计数；敏感词扫描和 health access 日志计数均为 0。

## Phase 9：真实内容接入与资讯化升级

### 网页材料、合规采集与来源

- [x] 通过 V5 Flyway 迁移增加 `WEB_ARTICLE` 元数据、图片来源字段、`source_registry` 和 `crawl_job`，不修改历史迁移。
- [x] 增加手工 URL 预览/确认导入；预览不落正式材料，确认后保存真实 HTML、正文、canonical URL、正文 SHA-256 和真实 segment。
- [x] 增加手动重新采集；重新检查白名单、robots 和正文 hash，保留已发布内容保护，正文变化后清理旧处理结果并重新进入待处理状态。
- [x] 仅允许启用的完整域名白名单；检查 robots.txt、公开 IP、User-Agent、单域名限速、超时和连接复用，不绕过受限页面。
- [x] canonical URL 与正文 hash 去重；相同预览短期缓存，不重复创建公开内容。
- [x] 修复网页导入 SQL 占位符错位导致 URL 被写入 `import_method` 的 500 错误。
- [x] 修复真实网页 `<meta charset>` 缺少 `property/name/itemprop` 时的解析异常。
- [x] 修复网页无文件 SHA-256 时错误共用 `"null"` AI 缓存键；网页现使用真实正文 `content_hash`，上传文件仍优先使用文件 SHA-256。
- [x] `JIANDA_ALLOW_FAKE_IP_DNS` 默认关闭；仅本地代理/VPN 把公网域名映射到 `198.18.0.0/15` 时可显式开启，生产环境和其他私网、回环、保留地址仍拒绝。

### 内容类型、封面和审核

- [x] 支持 `SERVICE_NOTICE`、`HEALTH_EDUCATION`、`POLICY_NEWS`、`ANTI_FRAUD`、`COMMUNITY_SERVICE`、`GENERAL_NEWS` 分类；服务通知继续走 v1.1 公共服务 Schema，其余网页文章走资讯安全 Prompt。
- [x] 封面优先级为 OpenGraph → JSON-LD → 正文有效首图；过滤 Logo、二维码、广告、头像、图标、追踪像素、过小和不适合卡片比例的图片。
- [x] 白名单未明确允许图片下载时不请求原图，直接使用本地分类默认图；允许时才校验 HTTP 200、`image/*`、尺寸、比例与 SHA-256。
- [x] 保存封面类型、图片来源、alt、缓存状态、许可说明、宽高和 hash；不自动生成 AI 图片。
- [x] 健康科普、养老政策、防诈提醒、社区服务、文化学习和办事通知共六张本地中性默认图已加入用户端。
- [x] 机构审核页显示当前封面、来源、类型、缓存状态和官方原文，支持确认原图或更换为分类默认图；第三方封面未经确认后端拒绝发布。
- [x] 首页主图、资讯卡片和详情封面使用固定比例、`object-fit: cover`、准确 alt 和失败回退；列表图懒加载，官方原文按钮使用文章 URL。

### 验证状态

- [x] AI 全套 58 项通过，覆盖 OpenGraph 优先、JSON-LD 回退、正文首图、Logo/二维码/追踪像素排除、404 回退、无下载许可不请求原图、分类和 Prompt 路由。
- [x] 后端全套 22 项及打包通过；集成回归覆盖白名单、去重、重新采集、正文 hash 缓存键、默认图、许可原图、图片来源保存和第三方图片发布门禁。
- [x] 两个前端类型检查和生产构建通过；Docker/Nginx 与 375px 首页封面失败回退、懒加载、比例、无横向溢出、官方原文跳转 Playwright 共 10 项通过。
- [x] 7 篇 2026 年 7 月真实内容已逐篇预览、导入并使用 External Provider 处理；初始验收时文档 27–33 均为 `WAITING_REVIEW`、`cache_hit=false`，每篇生成 7 类内容，未自动审核或发布。
- [x] 文档 27–33 分类依次为 `HEALTH_EDUCATION`、`POLICY_NEWS`、`POLICY_NEWS`、`COMMUNITY_SERVICE`、`HEALTH_EDUCATION`、`HEALTH_EDUCATION`、`ANTI_FRAUD`；字段数依次为 4、1、4、2、3、4、4。
- [x] 文档 27–33 Token 用量依次为 3956、1901、3053、2183、2175、2384、2882；总耗时依次为 13339、5747、9710、6137、6728、6775、10575 ms。
- [x] 第 7 篇官方 HTTPS 地址在当前本地代理/OpenSSL 组合下出现 `BAD_ECPOINT`；同一官方站点公开 HTTP 入口通过白名单与 robots 检查后导入，未绕过登录、验证码或访问控制。
- [x] Docker 三个应用镜像已重建，MySQL、AI、后端和前端四服务均为 healthy；AI 容器确认使用 External Provider，未读取或输出模型密钥。
- [x] 通过 V6 幂等迁移按 7 个 canonical URL 将 Phase 9 评测文章绑定到演示机构 2；文档 27–33 的 `created_by=1`、`source_type=WEB_ARTICLE`、segments、fields 和 generated content 均保持不变。
- [x] 机构端“上传材料”增加文件/网页双入口；`ORG_ADMIN` 可执行 URL 预览和导入，新文章自动绑定当前登录机构，`REVIEWER` 不显示导入入口。
- [x] 工作台、当前待办和材料列表统一显示 `WEB_ARTICLE` 来源、机构、分类及原始发布时间，并按真实状态计入统计。
- [x] 网页审核页展示正文快照、正文图片、分类封面、图片来源、官方 canonical URL、适老摘要和追溯字段；不请求 PDF 原文件，附属资源失败不再清空文档详情。
- [x] 文档访问已区分 403 跨机构无权与 404 记录不存在；网页文章无 `storage_path` 时原文件接口返回明确 404，不影响审核。
- [x] 文档 27 已使用现有 4 个 AI 字段完成确认、人工审核并发布为 `news-27`；用户端首页和资讯详情可见，官方原文仍指向新华网 canonical URL。
- [x] 发布前机构待审核 9 条；文档 27 发布后为 8 条，其中文档 28–33 共 6 篇 Phase 9 文章仍为 `WAITING_REVIEW`。
- [x] 后端全套 23 项与 `mvn package` 通过；机构端网页导入/列表/审核 Mock Playwright 3 项、封面回退 2 项、真实 Docker 审核/H5 Playwright 2 项通过。
- [x] 真实 390px 验收发现并修复 canonical URL 导致的横向溢出；机构端 1440×900、用户端 390×844 均无空白页、框架错误层、控制台错误或页面异常。
- [x] Docker backend/frontend 完成重建，V6 已在 `jianda` 应用；MySQL、AI、后端和前端四服务均 healthy，AI Provider 最终确认仍为 External，未输出模型密钥。
- [ ] 文档 28–33 的标题、日期、关键数字、医疗安全、原文链接和图片来源仍需机构管理员逐篇人工事实复核；完成前不得标记已审核或发布。
- [ ] 真实 iPhone Safari / Android Chrome 的远程图片失败回退和 24px 大字模式仍需人工抽检。

## Phase 9.1：资讯产品化、原文件阅读器与真实 URL 验收

- [x] 机构端和用户端统一接入共享 PDF.js 阅读器，移除 PDF iframe；支持页码、翻页、缩放、适合宽度、全屏、加载/重试、移动端双指缩放和独立下载。
- [x] 图片材料使用共享原图阅读器；在线读取保持 `inline`，下载使用 `attachment` 与原始文件名。
- [x] `original_file_available` 同时检查 PDF/IMAGE 类型、公开授权、存储路径和物理文件；WEB_ARTICLE 不再显示原 PDF。
- [x] 封面类型、来源等级、内容类型、状态和日期使用统一中文映射；技术枚举只放在折叠“技术信息”中。
- [x] H5 统一清理 Markdown/HTML/JSON/转义字符、标题来源后缀并截断摘要；首页重排为今日推荐、重要提醒、紧凑资讯、服务通知和分类入口。
- [x] 推荐卡限制图片比例、高度、标题和摘要行数；增加中性的综合资讯默认插图，图片保持比例、懒加载、alt 和失败回退。
- [x] 网页文章使用请求级 `web-v1.1`，兼容旧结果并增加关系、行动、关键事实、误区、FAQ、范围和不确定项，事实模块校验原文引用。
- [x] H5 资讯详情增加“快速看懂 / 完整解读”切换。
- [x] V7 记录原文/适老字符数、压缩率、事实/行动数、追溯率、人工修改、幻觉、Markdown 残留、耗时和 Token。
- [x] V8 预留自动采集、栏目 URL、链接选择器和最近错误；平台页展示网页白名单与采集任务并可停止等待/运行任务。
- [x] 正文未变化时标记 `UNCHANGED`、`cache_hit=true`，保留结果且不调用 DeepSeek；V10 支持网页版本关系，已发布内容变化时保留线上版本并自动创建、处理新的待审核版本。
- [x] 增加 `RUN_EXTERNAL_SMOKE=1` 可选真实机构端导入/External/审核/发布/H5 测试，默认自动测试不消耗真实 API。
- [x] AI 59 项、后端 Maven 全套和两端 typecheck/生产构建已通过；产品化 Mock 回归 8 项通过。
- [x] 使用数据库内动态选择的真实上传 PDF 完成桌面 1440×900、移动 390×844 阅读器验收；修复 `.mjs` MIME、PDF.js 私有字段被 Vue 深层代理以及旧上传记录文件名/MIME 回退，渲染、缩放、适宽和下载测试通过。
- [ ] 已选定当前数据库未导入的新华网白名单 URL；真实 External 调用与发布操作因执行环境要求再次明确授权而未运行，首次/缓存耗时和安全日志摘要待授权后补齐。

## Phase 9.3 — 采集运营、版本审核与连续阅读收口

### 已实现

- [x] V12–V16 增加来源运营、调度锁、任务错误队列、内容版本、图片候选、AI 队列和文章/Token 预算；历史 migration 未修改。
- [x] 支持 RSS、Atom、Sitemap/Sitemap index、JSON-LD 与栏目链接的白名单有界发现；保留 robots、SSRF、重定向、体积、深度、限速和去重边界。
- [x] 新来源默认停用、禁止图片缓存、禁止图片候选和自动 AI，并强制人工审核；启停操作需要确认。
- [x] 采集任务中心支持状态/来源筛选、计数摘要、单条与批量重试、取消、有限退避及自然语言冲突提示。
- [x] 已发布正文变化时创建 V2+ 待审核版本，旧版本继续公开；图片候选必须补齐来源和许可说明，未审核图片阻止发布。
- [x] H5 支持上一篇/下一篇、ArrowLeft/ArrowRight、移动端左右滑动、交互控件/文本选择隔离、首尾边界和阅读设置保持。
- [x] 自动调度与真实 AI 默认关闭；预算不足进入 `WAITING_BUDGET`，自动 AI 未开启进入 `WAITING_APPROVAL`，不会自动审核或发布。

### 已自动测试

- [x] 后端已有离线集成测试覆盖来源默认值、调度锁、发现适配器、任务错误队列、版本保护、图片审核、预算原子预留与恢复。
- [x] Playwright 截图默认写入 `testInfo.outputPath`；仅显式设置 `JIANDA_ACCEPTANCE_ARTIFACT_DIR` 才写指定目录，并使用 spec、视口、运行 ID、worker 与 retry 生成唯一文件名。
- [x] 新增来源调度预算、任务重试、版本图片审核、fixture 操作和 H5 大字连续阅读隔离验收，不访问真实后端、真实网页或真实 AI。
- [x] 收口实测：AI 71 项、Maven 45 项通过；机构端与 H5 的 typecheck/build 通过；默认全量 Playwright 75 项通过、9 项按安全条件跳过、0 项失败。
- [x] 全量 Playwright 前后对 `artifacts/phase7-3` 执行 SHA-256 比较，历史验收截图保持不变。
- [x] Docker 四个服务均为 healthy，`8001/health`、`8080/actuator/health`、`8090/health`、`80/health` 均为 HTTP 200。
- [x] 真实 External 烟雾与具有模型调用/业务写入副作用的全链路 E2E 默认跳过，分别要求显式设置 `RUN_EXTERNAL_SMOKE=1`、`RUN_MUTATING_E2E=1`。

### 待人工验收

- [ ] 平台管理员在 1440px 复核来源新增、编辑、启停、预算等待和任务错误详情的文字密度与操作反馈。
- [ ] 在 375px 真机复核 V2 审核、长图片 URL、分类默认图、大字模式与左右滑动，确认没有误触和横向滚动。
- [ ] 人工确认候选图片的版权/许可口径、来源命名和最终发布责任。

### 后续真实来源影子运行

- [ ] 选择已授权白名单来源进行只发现、不调用真实 AI、不发布的影子运行，记录命中率、重复率、robots 状态、失败分类和预算估算。
- [ ] 影子运行稳定并完成人工审批后，再评估 Phase 9.4 的生产调度开关；不得直接开启全量自动采集。
- [ ] 真实 iPhone Safari、Android Chrome 的 PDF 手势、全屏、下载和 24px 大字模式仍需人工抽检。

## Phase 9.4 — 明日验收前产品化、真实数据与真机兼容冲刺

### 已实现

- [x] 助手只检索 `PUBLISHED` 内容，contextSlug 优先，最多三条证据；无证据返回明确拒答。
- [x] 抽出可替换 `PublishedContentRetriever`，增加中文同义词扩展；状态问题由后端回答，
  有依据问题走已审核内容 + External RAG，低风险无依据问题可标注为通用 AI 参考，
  医疗诊断、资格、金额和材料等高风险问题无依据时拒绝猜测。
- [x] 增加受开关和每日次数/Token 预算保护的 External RAG；要求编号引用、行动建议和
  证据外事实禁令，失败安全降级为 retrieval。
- [x] 助手审计只保存问题粗分类、模式、证据/引用数、模型、请求 ID、Token、耗时和错误类型，
  不保存完整问题、证据正文或认证信息。
- [x] iPhone Safari 朗读改为不超过 120 字的标点分段，监听 voiceschanged，优先中文声音；
  暂停取消队列，继续重建当前段，带 watchdog、进度和离页停止。
- [x] 首页去除跨模块大面积重复，增加真实已发布内容驱动的权威来源、健康提醒和防诈提醒模块。
- [x] V17 新增助手查询、匿名浏览和每日运营快照；平台管理员工作台展示真实数据库运营指标，
  机构管理员无平台指标权限。
- [x] 分离 `allow_image_candidates` 与 `allow_image_cache`：前者生成机构端候选，后者
  只控制人工确认后的公开缓存；支持 OG、JSON-LD、懒加载属性、srcset、picture 和
  video poster，未审核图片继续回退分类默认图。
- [x] 权威来源页增加“发现文章 / 影子采集 / 立即采集”：前两者不创建材料且不调用 AI，
  后者只创建材料并进入 `WAITING_APPROVAL`，不自动审核或发布。
- [x] 新增仅平台管理员可见的 `/operations`，使用真实数据库展示来源、今日采集结果、
  AI 队列/预算、图片与内容审核量、平均耗时、最近错误和失败来源。
- [x] External Fact/Rewrite 增加通用 Schema 恢复：枚举别名归一化、可选字段安全补全、
  未知字段隔离、`uncertain_fields` 和 `repaired_paths`，不写死材料事实。
- [x] 新增快速/完整验证脚本、开发快速路径、清理候选和明日教师验收手册。
- [x] 材料处理改为立即返回任务信息的后台异步流程；处理页轮询并恢复真实阶段、章节、
  Token、耗时、失败详情和完成结果。
- [x] PDF 阅读器增加 Range/ETag、完整 Blob 回退和分级错误诊断，AI 提取失败不再等同于
  原文件预览失败。
- [x] 增加九类通用材料路由和长 PDF 章节化、有界并发、检查点、缓存键及多模块成功判定。
- [x] V18 增加高级调度、图片策略和来源身份；支持未知官网快速确认及按账号指纹区分
  官方微信公众号，不按 `mp.weixin.qq.com` 共享域名直接认定来源。
- [x] 权威来源页整合来源列表、扫描与导入、AI 队列、采集任务和高级自动采集；手动扫描
  支持时间范围、关键词、去重、多选和批量加入 AI 等待队列。
- [x] V19 兼容历史图片缓存许可；历史封面补齐支持网页候选重扫、PDF 第一页和上传图片，
  并强制先预览再执行。
- [x] H5 增加 30 套分类素材、稳定 hash、同类备用回退及移动端紧凑横向操作区。
- [x] `.env.example`、Compose 和本机 `.env` 已同步图片候选及历史补图非敏感开关；
  `scripts/sync-env.ps1` 只补缺失键、敏感键只补空值且不显示任何值。

### 已自动测试

- [x] AI 本地 Mock HTTP Server 覆盖有效引用、提示注入约束、token/耗时和无引用拒绝。
- [x] 后端集成测试覆盖 AI 模式、撤回内容排除、External 失败降级、真实运营聚合、每日快照
  和平台/机构管理员权限。
- [x] Playwright 隔离 mock 覆盖 AI/检索模式、行动建议、引用链接、无证据拒答和 iPhone
  分段朗读、暂停续播、中文声音、进度及离页停止。
- [x] 图片候选定向回归 17 项、来源三段式操作和运营指标后端集成测试通过；测试未调用
  真实网络或真实 Provider。
- [x] Schema recovery 定向测试覆盖事实与改写两阶段的别名、缺省、隔离、不确定字段和
  修复路径记录。
- [x] 历史补图/网页图片策略/核心发布后端定向测试 21 项通过；AI 封面与网页解析测试
  29 项通过；来源扫描、调度预算、历史补图与 H5 封面 Playwright 8 项通过。
- [x] 两端 typecheck/build 通过；375px 首页确认分类图片稳定、图片失败安全回退、
  操作区不再纵向拉长且无横向滚动。
- [x] 已发布、已审核和已撤回材料返回处理结果页时正确识别为成功终态，显示“处理完成”
  并保留原文对照审核入口；对应真实文档回归通过。

### 本轮最终验证

- [x] 断链修复收口验证：AI 94 项、Maven 58 项、两个前端 build/typecheck 全部通过。
- [x] 全量 Playwright 共 100 项：91 passed、9 个安全条件 skip、0 failed；异步处理、
  PDF 回退、材料结构、来源身份、采集重试、封面和 H5 组合流程均通过。
- [x] Docker 全量构建后 MySQL、AI、后端、前端四服务 healthy，四个健康接口 HTTP 200。
- [x] Phase 7.3 的 19 张历史截图运行前后 SHA-256 完全一致。
- [x] 本轮未调用真实 External Provider，未输出认证信息或密钥，也未修改本机现有
  External 能力开关。

### 待人工验收

- [ ] 在 iPhone Safari 和 Android Chrome 验证 24px、朗读、左右滑动、图片失败回退和局域网。
- [ ] 对至少两张第三方图片逐张确认来源与许可；确认前继续使用分类默认图，不把未知权利图片公开。
- [ ] 使用统一量表人工核对 1 份官方 PDF、2 篇真实网页、1 篇健康和 1 篇政务/养老内容，
  记录修改前后差异和真实修改率。
- [ ] 对现有白名单来源执行一次“只发现、不自动 AI、不发布”的有限影子运行并人工复核结果。
- [ ] 国家卫健委老年健康标准 PDF、中国老龄协会长篇政策、反诈长材料、视频封面和大文件
  边界仍需在取得明确测试文件或授权 URL 后逐项人工验收；本轮没有用单篇 smoke 冒充多材料验收。
- [ ] 在 Docker 真实历史数据上先预览后执行封面补齐，记录网页候选数、自动确认数、
  PDF 第一页封面数和失败原因；未执行前不得填写虚构数量。
- [ ] 对一个已授权来源完成有限手动扫描和一次正常频率调度观察，确认材料进入人工 AI/
  审核队列且没有自动发布；测试后只恢复来源业务频率，不关闭全局能力。
# Phase 9.4 大场镇真实社区验收冲刺（2026-08-23）

- [x] 扫描 PDF 使用本地 Tesseract 中文 OCR 逐页回退，文本 PDF 继续使用 PyMuPDF。
- [x] OCR 失败返回可操作错误；External 改写失败完整保留 Provider、模型、请求、Token 和耗时诊断。
- [x] 新增 V21 行政区划字段与默认停用的大场镇政府信息公开来源。
- [x] H5 增加大场镇区域状态、本地化首页和基于真实已发布内容的“邻里”入口。
- [x] H5 地区选择面板展示上海各区和宝山区街镇，仅大场镇可进入，其他地区明确“即将开通”。
- [x] Docker 内验证文本 PDF、多页 PDF 与由真实材料页面生成的扫描 PDF：分别走
  `pymupdf`、`pymupdf` 与 `ocr`，页码和 segment 数量一致。
- [x] 大场镇政府信息公开目录完成一次受控发现（5 个候选）和目标文章影子预览；来源已
  恢复默认停用，未创建材料、未调用 AI、未发布。栏目返回 9 条非致命解析错误，仍待人工
  观察与适配。
- [ ] 对大场镇目标文章执行人工确认、有限一次真实 DeepSeek、审核和发布验收。
- [x] 处理页展示并持久化正文提取方式，刷新后可区分文本层、OCR、混合提取和人工录入。
- [x] AI 97 项、Maven 62 项、两端 typecheck/build 通过；大场镇 H5 隔离测试 3 项通过。
- [x] 修正区域查询后的封面 mock，并单独复跑 `phase9-cover-images.spec.ts`：2 passed。
- [x] Phase 9.5 已统一复跑关键及全量 Playwright，历史入口断言已迁移到产品化信息架构。
- [x] Docker 重建后 Flyway 连续校验 25 个 migration，并从 V21 成功迁移至 V25；四服务 healthy。
- [x] 居民最小账号、个人资料、邻里互动和平台治理闭环。

## Phase 9.5 — 商业化产品化与操作减法（自动验收完成，待受控 External 人工验收）

- [x] 收口 Phase 9.4 合法文档和测试改动，建立独立 Phase 9.5 分支、基线审计与 checklist。
- [x] 保存真实修改前截图，未触碰 `artifacts/phase7-3` 历史验收证据。
- [x] 机构端一级导航从 7 项收束为工作台、内容中心、采集与来源、数据概览、系统记录。
- [x] 工作台只突出待审核、待发布、需要关注和按状态排序的今日待办。
- [x] 内容中心统一材料、网页与已发布状态；“添加内容”统一上传、网页链接和手工录入。
- [x] `VITE_ENABLE_DEV_FIXTURES` 默认关闭，正式 UI 不显示本地示例导入。
- [x] 机构端本轮 typecheck 通过、生产 build 通过；后台减法隔离 Playwright 2 passed。
- [x] 自动采集默认改为来源健康卡和一键检查，技术参数、预算、队列与任务收进平台高级设置。
- [x] 来源运营隔离 Playwright 3 passed，覆盖安全默认值、375/1440 无溢出和发现/影子/立即采集三段控制。
- [x] 风险聚焦审核与普通/大字双发布预览；隔离 Playwright 3 passed。
- [x] V23 增加生效、截止、过期、核验和来源更新时间；过期内容退出列表但详情保留，原文变化进入待复核。
- [x] H5 首页形成要紧事项/大场通知/长辈常用/最近更新；真实字段服务目录和游客提醒闭环通过隔离测试。
- [x] BCrypt 居民 DEMO 账号、纯文字邻里发布/点赞/评论/举报、平台隐藏/恢复与后端大场镇范围控制。
- [x] V24 增加匿名提醒和最小使用事件，运营看板展示真实周数据与内容排行。
- [x] V25 增加 BCrypt 居民账号、哈希会话、帖子、点赞、评论、举报和治理状态。
- [x] H5 PWA manifest 与本地图标完成；未注册 Service Worker，避免缓存身份和动态业务数据。
- [x] 375/390/768/1440 多视口、24px 大字、统一入口、PDF 阅读器和风险审核回归完成。
- [x] Docker 四服务 healthy，四个健康接口 HTTP 200，V23—V25 已在 MySQL 实际应用。
- [x] 17 张修改后产品化截图和索引、Phase 9.5 产品报告、测试报告及迁移说明完成。
- [ ] 真实大场镇网页 External AI → 审核 → 发布仍需单次显式授权的受控人工验收；本轮未调用真实 DeepSeek。

## Phase 9.6 — 真实全链路验收、真实配图、助手升级与商业视觉跃迁（自动收口完成，待真机验收）

- [x] 审计 Phase 9.5 本地与远端基线；本地与远端 ahead/behind 均为 0。
- [x] 从 `0e1c64b` 建立 `feat/phase9-6-real-acceptance-v1`，未覆盖 Phase 7.3 的 15 张现有修改截图。
- [x] 建立 `PHASE9_6_BASELINE_AUDIT.md`、`PHASE9_6_CHECKLIST.md`、真实验收截图目录和问题台账。
- [x] 完成真实 MySQL 图片基线审计：21 条已发布内容均为分类默认图，真实封面覆盖率 0%，1 个候选因使用依据未确认而阻塞。
- [x] 保存首页、详情、助手、采集来源和工作台 before 截图；截图来自真实 Docker 页面且未拦截 API。
- [x] V26 增加候选上下文和相关性分数；通用解析结合 DOM、附近正文、标题主题和发现方式，不包含站点专用选择器。
- [x] 首页只在同一重要性分段内对真实封面加权；无真实图片或破图时使用文字 Hero，分类 SVG 不再占据主视觉。
- [x] 对 4 篇已发布新华网文章执行真实历史重扫；首次发现并修复 2 个推荐图误候选，清缓存复扫后有效候选为 0，未伪造封面且来源开关已恢复。
- [x] 修复真实部门目录把导航链接当文章的问题；大场镇真实 discover 在最近 30 天返回 4 篇文章、过滤 82 条导航并保留发布日期，shadow 正文长度 799、识别 7 个待验证图片元素。
- [x] 大场镇文档 63 完成真实 collect → queue 19 → External job 72 → 自动化事实确认/人工修正 API → 发布 `news-63`；provider `external`、model `deepseek-v4-flash`、token 4077、耗时 10318 ms，无 Mock fallback。
- [x] 网页来源地区通用传播到材料和公开内容；文档 63 为 `310113102/STREET`，并提供既有文章安全同步接口。
- [x] 增加受全局开关控制的生产 `CrawlScheduler`；真实 job 29 发现 3、加入 3、失败 0，结束后恢复来源停用、自动采集关闭和 7 天窗口。
- [x] 助手增加确定性事实覆盖后检和已核对 factCards；证据外电话、日期、时间和金额安全回退，不直接返回用户。
- [x] 建立不拦截 API 的真实 Docker Playwright 套件与静态守卫；真实发布 1 passed，真实居民/邻里/提醒/运营 1 passed。
- [x] 真实居民 id 1、帖子 id 4 完成登录、发帖、点赞、评论、举报、管理员隐藏、居民不可见和恢复；周阅读 63→64、收听 0→1、提醒 0→1。
- [x] 官方 PDF 文档 67 完成 PyMuPDF 8 页/8 segments、真实 External、事实追溯、无关字段排除、审核与发布；公开为 `guide-67`。
- [x] 真实 DeepSeek 助手完成 10 个不同问题评估，12 次成功回答共 8833 Token；安全拒绝和 retrieval 回退未冒充 External PASS。
- [x] 文档 63 官方候选 8 完成课堂 Demo 范围来源核对、人工确认和本地缓存；公开 cover 为 1949×1183 JPEG。
- [x] 搜索无结果可携带原关键词进入简达；四类首页/资讯布局保持独立信息层级。
- [x] 新增一键脚本 `scripts/run-phase9-6-real-acceptance.ps1`，包含 Docker、四健康检查、No-Mock guard、REAL Playwright、可选全量回归和 Phase 7.3 证据保护。
- [x] 本轮回归：AI 105、Maven 72、两端 typecheck/build、全量 Playwright 105 passed/16 skipped/0 failed（121 total）；Docker 四服务 healthy、四健康接口 HTTP 200。
- [x] Phase 7.3 的 15 张用户现有修改截图在全量测试前后组合 SHA-256 不变，未覆盖、暂存或提交。
- [x] after 截图已覆盖 375/390/768/1440 与 18/20/22/24px；可见真实图片自然宽高全部大于 0。
- [ ] PARTIAL：iPhone Safari / Android Chrome 的触控、朗读、拨号仍待真机人工验收。
- [ ] PARTIAL：当前 Codex 进程未注入受保护平台/居民密码，一键 REAL Playwright 的 2 个认证场景明确跳过，不计 PASS。
- [ ] 未来公网或商业部署前重新进行真实图片版权审核；当前使用依据只覆盖本地、局域网和课堂 Demo。
- [x] 已分别输出 Regression Tests 与 REAL ACCEPTANCE；当前最终结论为 `REAL ACCEPTANCE: PARTIAL`，未把 Mock/fixture 通过数计入真实验收。
