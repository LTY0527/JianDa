# Phase 9.9 采集交互验收报告

日期：2026-08-24

## 已完成

- “立即检查”创建异步 discovery job 后立即进入 `/public-sources/:sourceId/check/:jobId`，不再把结果埋在来源设置长页下方。
- 运行中显示连接官网、读取栏目、识别文章、去重和整理结果五步状态，并明确说明离开页面不会中断任务。
- 完成后分别显示发现、已有、新增数量；导航、目录和非白名单链接只显示聚合过滤数，不在普通视图重复错误。
- 候选结果支持单条加入、多选和全选未重复内容；任何采集都不会自动发布。
- 加入后提供“立即处理”“继续查看”“返回来源”三个明确下一步；单篇导入可直接进入材料处理页。
- 失败页提供重新检查、粘贴文章链接、上传 PDF 和返回来源；技术错误码收进折叠详情。

## 自动验证

- 机构端 `npm run typecheck`：通过。
- 机构端 `npm run build`：通过，Vite 生产构建完成。
- `docker compose build frontend` 与 `docker compose up -d frontend`：通过，容器重建且未删除 volume。
- Playwright `立即检查进入独立进度页并引导加入内容中心`：1/1 通过。

## 边界

- 本轮自动测试使用隔离 API 响应验证 UI 状态机，因此仅计入 UI 自动验收，不冒充真实官网采集 PASS。
- 真实来源的运行耗时、候选质量和失败恢复仍需在 Phase 9.9 REAL browser 中验证。
