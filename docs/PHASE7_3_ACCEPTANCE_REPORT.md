# Phase 7.3 验收报告

更新时间：2026-07-23

## 结论

**通过：可以进入 Phase 8**

Phase 7.3 范围内未发现 P0，发现的 8 个 P1 和 6 个高性价比 P2 均已修复并完成定向回归。Phase 8 仍需由用户明确启动，本轮未修改部署配置。

## 验收范围

- 核心导入、AI 处理、原文对照、人工确认、审核、发布、H5 展示、助手引用、收藏、历史、设置、下载和撤回闭环。
- 用户端五个一级页面、两类详情、原文页、直接访问安全返回、网络与语音降级。
- 机构端登录、工作台、材料、审核、来源、导入、发布、撤回、权限不足和真实操作日志。
- 375×812、768×1024、1440×900 三档视口；48px 触控、键盘焦点、图标可读名称和固定区域遮挡。

## 验收环境

- 分支：`main`
- Browser 插件：不可用；按前端调试规范使用仓库 Playwright Chromium、DOM 断言、下载事件和真实截图。
- Node.js：24.14.0；JDK：17；Maven：3.9.16。
- AI 虚拟环境：Python 3.13.5；项目最低支持 Python 3.11。
- 地址：机构端 5173、用户端 5174、后端 8080、AI 服务 8001。

## 自动化结果

| 检查 | 命令 | 真实结果 |
|---|---|---|
| 前端类型检查 | `npm run typecheck` | 通过，两个 Vue 工作区均无类型错误，7.4 秒 |
| 前端生产构建 | `npm run build` | 通过，两端构建成功，无大 chunk 警告，17.0 秒 |
| 浏览器完整 E2E | `npm run test:e2e` | 第二次完整运行 15/16 通过；唯一 40px 触控失败修复后定向 1/1 通过，全部 16 项获得通过证据 |
| 核心发布闭环 | `npm run test:e2e -- core.spec.ts --grep "平台管理员导入"` | 通过，发布、下载、助手、收藏、历史、撤回与撤回过滤完整验证 |
| 后端测试 | `mvn test` | 第二次全量 7/8 通过；唯一旧 HTTP 状态断言修正后 `PublicImportIntegrationTest` 1/1 通过，全部 8 项获得通过证据 |
| AI 失败专项 | `CoreFlowIntegrationTest#aiFailureMarksDocumentAndJobFailedWithNaturalMessage` | 通过，HTTP 503、材料 FAILED、任务 FAILED 和错误原因均正确 |
| 后端打包 | `mvn package -DskipTests` | 通过，生成 `backend-0.1.0.jar`，Maven 用时 4.753 秒 |
| AI 模块与依赖 | 模块导入、`pip check` | 通过，`AI import OK`，无损坏依赖 |
| AI 测试 | `pytest -q` | 4/4 通过，0.50 秒；仅有第三方 SWIG 弃用警告和受限环境缓存警告 |
| 四服务冷启动 | 5173/5174/8080/8001 | 全部 HTTP 200 |
| 停止后端口检查 | `scripts/stop.ps1` | 仅停止 JianDa 命令行进程树，四端口全部释放 |

完整 E2E 和 Maven 全量命令各最多重试两次；达到上限后仅对唯一剩余失败项做等价定向验证，没有隐藏失败或声称未执行的命令通过。

## 浏览器验收结果

- 五项底部导航、中央助手入口、一级页无返回、二级页 fallback、刷新和直接访问均可用。
- 首页不再出现时间戳测试标题；办事、资讯、助手、收藏、历史和撤回过滤使用真实公开接口。
- 原文下载生成 `text/plain;charset=utf-8` 文件，文件非空、文件名自然，成功和不可下载状态均有中文反馈。
- 18/20/22/24px、语速、高对比、频道偏好、最近浏览、收藏和历史均通过刷新持久化验证。
- 后端、助手请求超时、内容不存在、原文不存在和语音不支持时无白屏或无限加载。
- 机构端操作日志已移除静态数组，页面行数与真实接口一致。
- 最终截图人工核对未发现横向溢出、固定区域遮挡、空白摘要、随机长数字或明显错位。

## 缺陷统计

| 级别 | 发现 | 已修复 | 未修复 |
|---|---:|---:|---:|
| P0 | 0 | 0 | 0 |
| P1 | 8 | 8 | 0 |
| P2 | 6 | 6 | 0 |
| P3 | 0 | 0 | 0 |

详细记录见 [PHASE7_3_ISSUES.md](PHASE7_3_ISSUES.md)。

## 数据清理

- 通过 `import_url` 精确匹配 `/phase7-13位时间戳`，清理 15 条可复现的 E2E 文档及其关联任务、字段、生成内容、审核、发布项、收藏和操作日志。
- 清理后数据库查询剩余 0，公开列表与机构材料截图均无随机长数字。
- 清理前尝试复制在线 H2 文件到系统临时目录，但文件锁导致备份失败；删除仅涉及可重建的测试记录，因此这些记录不可从仓库恢复，正常演示数据未删除。

## 截图证据

真实运行截图位于 `artifacts/phase7-3/`，共 19 张，包括：

- 用户端：首页、资讯、助手、办事、我的、办事详情、资讯详情、原文页、下载成功状态、768px 首页和 1440px 首页。
- 机构端：登录、工作台、材料列表、原文审核、来源管理、公开信息导入、操作日志和发布成功。

## 环境限制与人工确认

- Browser 插件不可用，已使用 Playwright Chromium 完成等价验证。
- iPhone Safari、Android Chrome、系统字号、麦克风授权和真机语音仍需真实设备确认。
- 老年用户可发现性、触摸手感以及医疗、反诈、政策正式口径仍需目标用户和业务人员确认。

这些项目属于设备与业务验收，不是当前已知代码缺陷。

## Git 状态与提交

- 当前分支：`main`。
- 最终工作树：干净，`nothing to commit, working tree clean`。
- 相对 `origin/main`：领先 8 个提交。
- Phase 7.3 提交：
  - `test(验收): 建立Phase 7.3人工检查与缺陷分级清单`
  - `fix(h5): 修复原文下载与适老访问体验`
  - `fix(admin): 接入真实操作日志并补齐可访问性`
  - `fix(backend): 保留AI失败任务状态并返回正确状态码`
  - `fix(h5): 完善资讯降级与核心触控区域`
  - `fix(admin): 优化材料列表时间与完成状态`
  - `test(回归): 完成Phase 7.3核心流程与三档验收`
  - `docs(验收): 完成Phase 7.3验收报告与进入结论`

本轮未 push、未打 tag。

## Phase 8 建议

代码、测试、截图和验收文档已达到进入 Phase 8 的条件。进入部署前应先完成真实移动设备抽检，并由业务人员确认医疗、反诈和政策文案。
