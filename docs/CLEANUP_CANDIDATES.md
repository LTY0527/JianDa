# 清理候选（本轮不删除）

以下项目只记录为后续候选，不在 Phase 9.4 验收冲刺中批量清理：

- 合并 `README.md` 中较早阶段重复的运行说明，保留历史结论链接。
- 降低后端测试默认 DEBUG 日志量，失败时再开启详细 SQL 日志。
- 为前端较长的单文件样式按产品区域拆分，避免验收前进行无收益重构。
- 统一测试 fixture 的公开内容构造器，减少各 Playwright spec 重复 mock。
- 评估将每日运营快照改为独立定时任务；当前按平台管理员读取时幂等保存。
- 在完成版权人工确认后，清理不再使用的图片候选记录。

禁止把 `.env`、上传材料、真实网页全文、历史 Phase 7.3 截图、数据库 volume
或仍在使用的迁移列为自动删除对象。
